import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.ppt.u1',
  appName: 'ph-ppt-u1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
